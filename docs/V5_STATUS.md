# V5 Status

| phase | status | evidence | next gate |
| --- | --- | --- | --- |
| V5-0 Repository sanitation | PARTIAL | Inventory found 14,628 tracked `node_modules/` paths and 10 tracked `dist/` paths. The branch-specific, allowlisted sanitation workflow and hygiene validator are present; required `public/models/` and `public/wasm/` assets remain tracked. | Push this correction to `v5/v5-0-repository-sanitation`, confirm the automatic sanitation commit, and verify all four generated-path counts are zero. |
| V5 clean lockfile build | PARTIAL | Lockfile excludes `@svenflow/micro-handpose`, pins Three.js 0.170.0, and CI project installs use `npm ci`; local clean install is blocked by this environment returning HTTP 403 for public npm tarballs. | Run two clean `npm ci` + quality pipelines on a clean GitHub Actions runner and compare lockfile plus integrity manifest hashes. |
| V5 ONNX monocular depth | PARTIAL | Optional model bytes are SHA-384 verified against the build integrity manifest before transfer to the worker; WebGPU falls back to bundled ORT WASM, one-frame backpressure is enforced, and depth tensors feed the occlusion fragment pass with privacy-safe percentile/provider/tier diagnostics. No model binary is present in this branch. | Supply the provenance-reviewed `depth_anything_v2_small.onnx` through the release asset process, enable `VITE_ENABLE_MONOCULAR_DEPTH`, and validate WebGPU/WASM visual occlusion on clean Actions plus target hardware. |
| V5 gemstone mesh connection | PARTIAL | Runtime semantics prioritize `extras.materialRole`, gemstone metadata, stable names, then PBR; WebGPU assigns node materials with Cauchy dispersion, Beer-Lambert absorption, Fresnel/TIR and quality-scaled caustics, while WebGL receives physical approximations. The semantic asset gate proves the shipped GLB still has only one metal mesh. | Re-export the source model with separate Metal and Gemstone nodes (or provide an authored node mapping), run `semanticize-ring-glb.mjs`, then validate WebGPU/WebGL pixels on target hardware. |
